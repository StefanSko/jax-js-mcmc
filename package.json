{
  "name": "jax-js-mcmc",
  "version": "0.1.0",
  "type": "module",
  "license": "MIT",
  "peerDependencies": {
    "@jax-js/jax": ">=0.1.0"
  },
  "devDependencies": {
    "typescript": "^5.4.0",
    "vitest": "^1.6.0"
  },
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "test:physics": "vitest run tests/physics",
    "test:posteriors": "vitest run tests/posteriors",
    "test:reference": "vitest run tests/reference",
    "test:ci": "bash scripts/ci.sh"
  }
}
