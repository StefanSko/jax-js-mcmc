# Browser Example (Vite)

This is a minimal Vite app that runs HMC in the browser using WebGPU when available.

## Setup

```bash
cd examples/browser
pnpm install
pnpm dev
```

Open the printed local URL (typically http://localhost:5173).
